{
  "name": "spm-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --openssl-legacy-provider index.js",
    "start": "node --openssl-legacy-provider index.js",
    "test": "vitest --run --dir test --environment=node"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "googleapis": "^118.0.0",
    "safe-pocket-map-monorepo": "file:..",
    "serverless-http": "^3.0.0"
  },
  "devDependencies": {
    "supertest": "^6.3.3",
    "vitest": "^1.4.0"
  }
}
